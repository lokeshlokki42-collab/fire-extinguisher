import { useState } from 'react'; import { Flame, Menu, X } from 'lucide-react';
export default function Navbar({ onOrder }) { const [open, setOpen] = useState(false); const links = [['Product', '#product'], ['Why IMSAFE - Kavasam Safety Solutions', '#why'], ['Applications', '#applications'], ['Certification', '#certification']]; return <header className="navbar"><a className="logo" href="#top"><span className="brand-mark"><Flame size={17} /></span><span>IMSAFE - Kavasam Safety Solutions<small>LIFE MATTERS</small></span></a><nav className={open ? 'nav-links open' : 'nav-links'}>{links.map(([label, href]) => <a key={label} href={href} onClick={() => setOpen(false)}>{label}</a>)}<button className="button button-red nav-cta" onClick={onOrder}>Order now <span>↗</span></button></nav><button className="menu-button" onClick={() => setOpen(!open)} aria-label={open ? 'Close menu' : 'Open menu'}>{open ? <X /> : <Menu />}</button></header>; }
